name: First Workflow
on: workflow_dispatch
jobs:
  runs-on:windows-latest
  steps:
    -name: print greetings
     run: echo "hello world"
    -name: print goodbye
     run: echo"done-bye!"

    
      
